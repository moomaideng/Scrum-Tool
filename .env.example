# The host port can be changed without rebuilding the image.
HOST_PORT=4173

# Standup runs independently at http://localhost:4174/standup/.
# Copy the remaining values from standup/.env.example when configuring integrations.
STANDUP_HOST_PORT=4174
STANDUP_APP_ORIGIN=http://localhost:4174
STANDUP_DISCORD_WEBHOOK_URL=
